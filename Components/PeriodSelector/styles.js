import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  navigationPanel: {
    display: "flex",
    flexDirection: "row",
  },
  navigationButton: {
    width: 30,
  },
  navigationLabel: {
    flex: 5,
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 20,
  },
});

export default styles;
