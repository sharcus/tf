import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  periodLabel: {
    fontWeight: "bold",
  },
  textContainer: {
    display: "flex",
    flexDirection: "row",
  },
});

export default styles;
